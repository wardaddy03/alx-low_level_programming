#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string to be tested
 * Return: 0 or 1
 */
int _strlen(char *s)
{
	int count = 0;

	count = strlen(s);

	return (count);
}
